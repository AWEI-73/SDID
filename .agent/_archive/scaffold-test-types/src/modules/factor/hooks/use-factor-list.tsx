// src/modules/factor/hooks/use-factor-list.tsx (由 plan-to-scaffold 自動生成 | type: HOOK)

import { useState, useEffect } from 'react';

/** GEMS: useFactorList | P1 | CALL_API→UPDATE_STATE→RETURN */
// src/modules/factor/hooks/use-factor-list.tsx
// [STEP] CALL_API
// [STEP] UPDATE_STATE
// [STEP] RETURN
export function useFactorList(/* TODO */) {
  const [state, setState] = useState(null);

  throw new Error('Not implemented — Story-1.1');

  return { state };
}
