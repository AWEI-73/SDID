# Task-Pipe 架構簡報

**版本**: 1.2 | **日期**: 2026-02-09 | **對象**: 資深工程師 / 架構師

---

## 1. 問題陳述

### 1.1 AI 輔助開發的現況問題

| 問題 | 說明 |
|------|------|
| **AI 容易走歪** | 給 AI 一個需求，它可能產出 500 行 code，但方向錯誤 |
| **缺乏驗證機制** | AI 產出後，人類需要逐行 review，成本高 |
| **Context 爆炸** | 專案變大後，AI 無法理解全貌，產出品質下降 |
| **無法追蹤** | AI 改了什麼、為什麼改、影響範圍？沒有記錄 |

### 1.2 現有解決方案的不足

| 方案 | 做法 | 問題 |
|------|------|------|
| `.cursorrules` | 規則檔，告訴 AI 怎麼寫 | AI 不一定遵守 |
| `CLAUDE.md` | 專案說明 | 只是文件，沒有強制力 |
| Prompt Engineering | 精心設計 prompt | 每次都要重寫，不可複用 |

---

## 2. Task-Pipe 的解法

### 2.1 核心理念

```
腳本輸出指令 → AI 執行 → 腳本驗證 → 重複直到通過
```

**關鍵差異**：不是「建議」AI 怎麼做，而是「強制」AI 照步驟走，每步都有 checkpoint。

### 2.2 四階段工作流程

```
Blueprint Architect (5 輪對話) → Enhanced Draft
        ↓
POC (概念驗證) → PLAN (規格設計) → BUILD (實作測試) → SCAN (品質掃描)
        ↓                                                    ↓
        └──────────── iteration_suggestions ←─────────────────┘
```

| 階段 | 目的 | 產出 | 驗證點 |
|------|------|------|--------|
| **POC** | 消除模糊、定義契約 | 需求規格 + UI 原型 + 資料契約 | 功能清單明確、契約完整 |
| **PLAN** | 拆解成可執行任務 | Implementation Plan | 架構合規、標籤規格完整 |
| **BUILD** | 實作 + 測試 | Code + 測試 + Fillback | 型別檢查 0 error、測試 100% |
| **SCAN** | 全專案掃描 | Semantic Contract | 標籤覆蓋率、規格一致性 |

### 2.3 錨點系統 (Anchor System)

所有腳本輸出使用統一格式，讓 AI 和 Orchestrator 都能解析：

```
@CONTEXT   - 當前狀態
@TASK      - 需要執行的任務
@TEMPLATE  - 可複製的模板
@OUTPUT    - 產出位置 + 下一步指令
@PASS      - 通過，可進入下一步
@BLOCKER   - 阻塞，需人類介入
```

**這是 Orchestrator 的基礎**：機器可以解析這些錨點，自動決定下一步。

---

## 3. 執行模式

### 3.1 三種執行方式

| 模式 | 說明 | 人類介入 | 適用場景 |
|------|------|----------|----------|
| **CLI 手動** | 人類跑腳本 → 複製貼給 AI → 人類跑下一步 | 高 | 學習、Debug |
| **IDE 半自動** | IDE (Kiro/Antigravity) 自動讀錨點執行 | 中 (按 Accept) | 日常開發 |
| **CLI 全自動** | 腳本直接呼叫 LLM API，無人值守 | 低 (只看結果) | 批次執行、CI/CD |

### 3.2 IDE 半自動模式 (已驗證)

```
Antigravity 讀取 @TASK → AI 執行 → 人類按 Accept → 讀取 @OUTPUT 的「下一步」→ 重複
```

**實測結果**：POC → PLAN → BUILD 全流程可在 30 分鐘內完成一個小型專案，人類只需按 Accept 和偶爾救援。

### 3.3 CLI 全自動模式 (規劃中)

```javascript
// 概念架構
while (hasNextStep) {
  const output = exec(`node runner.cjs --phase=${phase} --step=${step}`);
  const task = parseAnchor(output, '@TASK');
  
  if (output.includes('@PASS')) {
    nextStep = parseAnchor(output, '@OUTPUT');
    continue;
  }
  
  const response = await claude.messages.create({ messages: [{ content: task }] });
  writeFiles(response);
  
  // 重跑驗證
  const verify = exec(`node runner.cjs --phase=${phase} --step=${step}`);
  if (verify.includes('@PASS')) nextStep = parseAnchor(verify, '@OUTPUT');
  else retryCount++;
}
```

**預估工作量**：200-300 行 code，主要是錨點解析 + API 呼叫 + 檔案寫入。

---

## 4. GEMS 標籤系統

### 4.1 為什麼需要標籤？

| 問題 | 標籤解法 |
|------|----------|
| AI 不知道函式重要性 | `P0-P3` 優先級標記 |
| AI 不知道執行流程 | `GEMS-FLOW` 步驟定義 |
| AI 不知道依賴關係 | `GEMS-DEPS` 依賴標記 |
| AI 不知道測試狀態 | `GEMS-TEST` 測試覆蓋 |

### 4.2 標籤格式

```typescript
/**
 * GEMS: calculateTotal | P0 | ✓✓ | (items)→number | Story-1.1 | 計算總金額
 * GEMS-FLOW: ValidateInput→SumItems→ApplyDiscount→Return
 * GEMS-DEPS: [Internal.validateItems], [Shared.formatCurrency]
 * GEMS-TEST: ✓ Unit | ✓ Integration | - E2E
 */
export function calculateTotal(items: Item[]): number {
  // [STEP] ValidateInput
  // [STEP] SumItems
  // [STEP] ApplyDiscount
  // [STEP] Return
}
```

### 4.3 標籤的價值

1. **Function Slicing**：SCAN 產出含行號的 `functions.json`，AI 可以只讀 30 行而不是 500 行
2. **影響分析**：修改某函式時，透過 `GEMS-DEPS` 知道影響範圍
3. **自動驗證**：Scanner 檢查 `GEMS-FLOW` 步驟數 = `[STEP]` 錨點數

---

## 5. Semantic Contract Layer

### 5.1 問題

> 「我怎麼知道 Story-1.1 實作了哪些函式？哪些 UI 元素？用了哪些資料結構？」

### 5.2 解法

SCAN 階段產出 `CONTRACT.md`，整合：
- **Data Contracts** (`@GEMS-CONTRACT`)
- **UI Bindings** (`@GEMS-UI-BIND`)
- **Functions** (GEMS 標籤)

```markdown
## Stories → Implementation

### Story-1.1
**Functions:**
- `handleTransactionSubmit` (P0) - 處理交易表單提交
- `renderTransactionList` (P1) - 渲染交易明細清單

**UI Modules:** FinancialApp

**Data Contracts:** Transaction, Category
```

### 5.3 價值

- **需求追蹤**：每個 Story 都有對應的實作
- **Code Review**：快速了解 PR 影響哪些 Story
- **重構評估**：修改某函式會影響哪些 Story

---

## 6. 錯誤恢復機制 v2.0

### 6.1 策略漂移 (Strategy Drift)

重試不是單純重複，而是「維度的提升」：

```
Level 1 (1-3 次): 🔧 TACTICAL_FIX - 局部修補
    ↓ 失敗
Level 2 (4-6 次): 🔄 STRATEGY_SHIFT - 換個方式實作
    ↓ 失敗
Level 3 (7+ 次): ⚠️ PLAN_ROLLBACK - 回退 PLAN 階段
```

**核心理念**：第 4 次失敗時，不是繼續在同一個坑裡挖，而是換個方向。

### 6.2 四大模組

| 模組 | 功能 | 觸發時機 |
|------|------|----------|
| **retry-strategy** | 追蹤重試次數，決定策略層級 | 每次 `anchorError()` |
| **taint-analyzer** | 分析修改檔案的影響範圍 | 有 `changedFiles` 參數時 |
| **incremental-validator** | 建議只驗證受影響的檔案 | 配合 taint-analyzer |
| **backtrack-router** | 根據錯誤類型決定回溯目標 | Level 3 時 |

### 6.3 整合輸出範例

```
@TACTICAL_FIX (4/8) | 測試失敗
@STRATEGY_DRIFT | Level 2/3 | 🔄 STRATEGY_SHIFT
  策略: 換個方式 - 重新實作或拆分函式
@TAINT_ANALYSIS | 修改 2 個函式 → 影響 5 個依賴者
  受影響檔案: src/auth.ts, src/user.ts, src/api.ts...
@INCREMENTAL_HINT | 建議驗證範圍:
  - 標籤驗證: 檢查受影響檔案的 GEMS 標籤
  - 測試驗證: 跑受影響檔案的測試
修復後: node task-pipe/runner.cjs --phase=BUILD --step=5 ...
```

### 6.4 狀態追蹤

| 檔案 | 內容 |
|------|------|
| `.task-pipe/state.json` | 當前階段/步驟/Story |
| `.gems/iterations/iter-X/.strategy-state.json` | 策略漂移狀態 (重試次數、層級、歷史) |

### 6.5 設計價值

- **避免無限重試**：有明確的升級機制
- **精準修復**：染色分析告訴 AI 影響範圍
- **減少浪費**：增量驗證不用從頭跑
- **智能回溯**：Level 3 時自動建議回到哪個階段

---

## 7. 技術架構

### 7.1 目錄結構

```
task-pipe/
├── runner.cjs              # 主入口
├── phases/                 # 階段腳本
│   ├── poc/               # POC Step 1-5
│   ├── plan/              # PLAN Step 1-5
│   ├── build/             # BUILD Phase 1-8
│   └── scan/              # SCAN
├── lib/                    # 核心函式庫
│   ├── shared/            # 狀態管理、錯誤處理、輸出格式
│   │   ├── state-manager-v3.cjs
│   │   ├── error-handler.cjs
│   │   ├── log-output.cjs      # 錨點輸出 (整合錯誤恢復)
│   │   ├── retry-strategy.cjs  # ⭐ 策略漂移
│   │   ├── taint-analyzer.cjs  # ⭐ 染色分析
│   │   ├── incremental-validator.cjs  # ⭐ 增量驗證
│   │   └── backtrack-router.cjs       # ⭐ 回溯路由
│   ├── scan/              # 標籤掃描、Contract 生成
│   └── build/             # 編碼驗證、UI Bind 驗證
├── tools/                  # 工具腳本
│   ├── draft-parser.cjs        # ⭐ v3.5 Enhanced Draft 解析器
│   ├── blueprint-runner.cjs    # ⭐ v3.5 藍圖執行器
│   ├── blueprint-architect.cjs # ⭐ v3.5 藍圖架構師
│   ├── blueprint-visualizer.cjs # ⭐ v3.5 藍圖視覺化
│   └── blueprint-studio.html   # ⭐ v3.5 互動式藍圖工作室
├── skills/                 # 技能插件
│   ├── ralph-loop/        # 自動執行迴圈
│   └── blueprint-architect/ # ⭐ v3.5 藍圖架構師 (5 輪對話)
├── templates/              # 模板
│   ├── enhanced-draft-golden.template.md  # ⭐ v3.5 黃金模板
│   └── examples/
│       └── enhanced-draft-ecotrack.example.md  # ⭐ v3.5 範例
└── mcp/                    # MCP Server (跨 IDE)
```

### 7.2 零依賴設計

- 只需 Node.js，不需要 `npm install`
- 所有腳本都是 `.cjs`，可直接執行
- 複製資料夾就能用

---

## 8. 與現有工具的整合

| 工具 | 整合方式 |
|------|----------|
| **Kiro** | Steering 規則 (`.kiro/steering/`) |
| **Cursor/Windsurf** | CLAUDE.md 模板 |
| **Claude Code** | MCP Server |
| **CI/CD** | CLI 全自動模式 (規劃中) |

---

## 9. 限制與取捨

### 9.1 已知限制

| 限制 | 原因 | 緩解方式 |
|------|------|----------|
| 只支援 TypeScript/JavaScript | 標籤解析器針對 TS/JS 設計 | 可擴展，但需要開發 |
| 需要人類救援 | AI 有時會卡住 | TACTICAL_FIX 減少頻率 |
| 學習曲線 | 標籤系統需要學習 | 提供 VSCode Snippets |

### 9.2 設計取捨

| 取捨 | 選擇 | 原因 |
|------|------|------|
| 彈性 vs 約束 | 約束 | 硬流程確保品質 |
| 速度 vs 驗證 | 驗證 | 每步都有 checkpoint |
| 簡單 vs 完整 | 完整 | 標籤系統雖複雜但價值高 |

---

## 10. 下一步規劃

| 優先級 | 項目 | 狀態 |
|--------|------|------|
| **P0** | ✅ 完整閉環 (Blueprint Architect → SCAN) | 已完成 v3.5 |
| **P0** | CLI 全自動 Orchestrator | 規劃中 |
| **P1** | Multi-Agent 並行開發 | 規劃中 |
| **P1** | 多語言支援 (Python) | 規劃中 |
| **P2** | Web Dashboard | 規劃中 |
| **P3** | GitHub Action 整合 | 規劃中 |

---

## 附錄：快速體驗

```bash
# 1. 建立專案
mkdir my-app && cd my-app
mkdir -p .gems/iterations/iter-1/poc

# 2. 建立需求草稿
echo "# 需求草稿\n\n## 一句話目標\n建立一個簡單的 Todo App" > .gems/iterations/iter-1/poc/requirement_draft_iter-1.md

# 3. 執行 POC Step 0
node ../task-pipe/runner.cjs --phase=POC --step=0 --target=.

# 4. 照著 @TASK 指示執行...
```

---

*Task-Pipe Framework | 2026-02-09*
