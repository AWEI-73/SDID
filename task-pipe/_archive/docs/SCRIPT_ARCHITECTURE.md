# Task-Pipe 腳本架構總覽

> 版本: v2.1 | 更新日期: 2026-01-30

---

## 📐 系統整體調用圖

```
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                            🔄 Ralph Loop (入口)                                              │
│                   .agent/skills/ralph-loop/scripts/loop.cjs                                 │
├───────────────────────────────────────────┬─────────────────────────────────────────────────┤
│  📋 Suggestion 模式 (預設)                │  📐 Blueprint 模式 (v2.1)                       │
│  驅動: iteration_suggestions.json         │  驅動: solution.mmap.yaml                       │
│  調用: runner.cjs                         │  調用: tools/blueprint-runner.cjs               │
└───────────────────────────────────────────┴─────────────────────────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                            📦 runner.cjs (主調度器)                                          │
│  職責: 解析參數 → 載入 phase-registry.json → 動態調用 phases/{phase}/{step}.cjs             │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
                                     │
        ┌────────────────┬───────────┴───────────┬────────────────┐
        ▼                ▼                       ▼                ▼
   ┌─────────┐      ┌─────────┐            ┌─────────┐       ┌─────────┐
   │   POC   │      │  PLAN   │            │  BUILD  │       │  SCAN   │
   │ step-1  │      │ step-1  │            │ phase-1 │       │  scan   │
   │ step-2  │      │ step-2  │            │ phase-2 │       └─────────┘
   │ step-3  │      │ step-3  │            │ phase-3 │
   │ step-4  │      │ step-4  │            │ phase-4 │
   │ step-5  │      │ step-5  │            │ phase-5 │
   └─────────┘      └─────────┘            │ phase-6 │
                                           │ phase-7 │
                                           │ phase-8 │
                                           └─────────┘
```

---

## 🔗 詳細階段調用關係

### 📂 POC Phase (概念驗證階段)

```
POC Step 0 (環境驗證)
├── require: fs, path
├── require: lib/shared/log-output.cjs
│   └── exports: anchorOutput(), anchorPass(), anchorError()
├── require: lib/shared/next-command-helper.cjs
│   └── exports: getNextCmd(), getRetryCmd(), getPassCmd()
└── 功能: 驗證 requirement_draft、偵測前一迭代 suggestions

POC Step 0.5 (快速原型)
├── require: lib/shared/log-output.cjs
├── require: lib/shared/output-header.cjs
│   └── exports: getOutputHeader(), getSimpleHeader()
└── 功能: 產生快速原型代碼

POC Step 1 (契約設計)
├── require: lib/shared/output-header.cjs
├── require: lib/shared/error-handler.cjs
│   └── exports: createErrorHandler(), MAX_ATTEMPTS
├── require: lib/shared/log-output.cjs
├── require: lib/shared/next-command-helper.cjs
└── 功能: 驗證 xxxContract.ts，包含 @GEMS-CONTRACT, @GEMS-TABLE, @GEMS-FUNCTION

POC Step 2 (UI 原型)
├── require: lib/shared/output-header.cjs
├── require: lib/shared/error-handler.cjs
├── require: lib/shared/log-output.cjs
├── require: tools/quality-check/poc-quality-checker.cjs   ⬅️ 品質檢查
│   └── exports: checkPocQuality(), extractFrontendSpecs()
├── require: skills/frontend-design/design-quality-checker.cjs  ⬅️ 設計品質
│   └── exports: checkDesignQuality()
├── require: lib/shared/next-command-helper.cjs
├── require: lib/step-result.cjs
│   └── exports: writeStepResult()
└── 功能: 驗證 xxxPOC.html，品質評分，自動產生智慧 POC

POC Step 3 (POC 總結)
├── require: lib/shared/log-output.cjs
├── require: lib/shared/next-command-helper.cjs
└── 功能: 產出 POC 總結報告
```

### 📂 PLAN Phase (規劃階段)

```
PLAN Step 1 (Blueprint 生成)
├── require: lib/shared/log-output.cjs
├── require: lib/shared/output-header.cjs
└── 功能: 產生 solution_blueprint.md

PLAN Step 2 (Story 分解)
├── require: lib/shared/log-output.cjs
├── require: lib/shared/error-handler.cjs
├── require: lib/shared/next-command-helper.cjs
└── 功能: 分解 Story，產生 implementation_plan

PLAN Step 2.5 (架構審查)
├── require: lib/shared/log-output.cjs
├── require: lib/shared/error-handler.cjs
└── 功能: 檢查架構一致性

PLAN Step 2.6 (風險評估)
├── require: lib/shared/log-output.cjs
└── 功能: 評估技術風險

PLAN Step 3 (PLAN 總結)
├── require: lib/shared/log-output.cjs
├── require: lib/shared/next-command-helper.cjs
└── 功能: 產出 PLAN 總結，準備進入 BUILD
```

### 📂 BUILD Phase (開發階段)

```
BUILD Phase 1 (開發腳本)
├── require: lib/checkpoint.cjs
│   └── exports: writeCheckpoint()
├── require: lib/shared/project-type.cjs
│   └── exports: detectProjectType(), getSrcDir(), getGreenfieldGuide()
├── require: lib/shared/output-header.cjs
├── require: lib/shared/error-handler.cjs
├── require: lib/shared/log-output.cjs
├── require: tools/quality-check/poc-quality-checker.cjs
│   └── exports: extractFrontendSpecs()
├── require: lib/shared/next-command-helper.cjs
├── require: lib/plan/plan-spec-extractor.cjs    ⬅️ [v3.0 新增]
│   └── exports: extractPlanSpec(), extractFunctionManifest(), getStoryContext()
└── 功能: 讀取 implementation_plan，產生程式碼骨架，注入 GEMS 標籤規格

BUILD Phase 2 (實作程式碼)
├── require: lib/shared/log-output.cjs
├── require: lib/shared/error-handler.cjs
├── require: lib/checkpoint.cjs
└── 功能: 驗證程式碼實作完成度

BUILD Phase 3 (整合驗證)
├── require: lib/shared/log-output.cjs
├── require: lib/shared/error-handler.cjs
├── require: lib/checkpoint.cjs
└── 功能: 驗證模組整合

BUILD Phase 4 (標籤驗收)  ⬅️ 重要！GEMS 標籤檢查
├── require: lib/shared/output-header.cjs
├── require: lib/checkpoint.cjs
├── require: lib/plan/plan-spec-extractor.cjs
│   └── exports: extractPlanSpec(), compareSpecs(), extractFunctionManifest()
├── require: lib/shared/project-type.cjs
├── require: lib/shared/error-handler.cjs
├── require: lib/shared/log-output.cjs
├── require: lib/shared/src-path-resolver.cjs
│   └── exports: resolveSrcPath()
├── require: lib/shared/next-command-helper.cjs
├── 動態載入: lib/gems-scanner.cjs (AST 版本)
│   └── fallback: lib/scan/gems-validator.cjs (Regex 版本)
│   └── GAS 專用: lib/gems-scanner-gas.cjs
└── 功能: 掃描 GEMS 標籤，計算覆蓋率，P0/P1 合規檢查

BUILD Phase 5 (測試驗證)
├── require: lib/shared/log-output.cjs
├── require: lib/shared/error-handler.cjs
├── require: lib/checkpoint.cjs
└── 功能: 執行測試，檢查測試覆蓋率

BUILD Phase 6 (整合測試)
├── require: lib/shared/log-output.cjs
├── require: lib/shared/error-handler.cjs
└── 功能: 執行整合測試

BUILD Phase 7 (整合檢查)
├── require: lib/shared/log-output.cjs
├── require: lib/shared/error-handler.cjs
└── 功能: 驗證路由整合

BUILD Phase 7 (完成規格)
├── require: lib/shared/output-header.cjs
├── require: lib/checkpoint.cjs
│   └── exports: clearCheckpoints()
├── require: lib/suggestions-validator.cjs
│   └── exports: validate()
├── require: lib/shared/error-handler.cjs
│   └── exports: handlePhaseSuccess()
├── require: lib/plan/plan-spec-extractor.cjs
├── require: lib/shared/project-type.cjs
├── require: lib/shared/log-output.cjs
├── require: lib/step-consistency-validator.cjs
│   └── exports: validateStepConsistency(), formatValidationResult()
├── require: lib/shared/next-command-helper.cjs
├── 動態載入: lib/gems-scanner.cjs
├── 動態載入: lib/stress-test-integration.cjs
│   └── exports: runRealProjectStressTest()
└── 功能: 產出 Fillback + iteration_suggestions，驗證可執行性
```

### 📂 SCAN Phase (掃描階段)

```
SCAN (全專案掃描)
├── require: lib/scan/gems-scanner.cjs
│   └── exports: scanDirectory(), parseFile()
├── require: lib/scan/gems-validator.cjs
│   └── exports: scanGemsTags(), validateP0P1Compliance()
└── 功能: 掃描全部 GEMS 標籤，產出 functions.json
```

---

## ⚡ 錯誤處理機制調用圖

```
任何 Phase/Step 失敗
        │
        ▼
┌──────────────────────────────────────────┐
│  lib/shared/error-handler.cjs            │
│  ────────────────────────────────────────│
│  createErrorHandler(phase, step, story)  │
│        │                                 │
│        ▼                                 │
│  errorHandler.recordError(code, msg)     │
│        │                                 │
│        ├─→ attempt < MAX_ATTEMPTS (3)    │
│        │   └─→ return TACTICAL_FIX       │
│        │       └─→ 顯示修正指引          │
│        │           └─→ 重跑同步驟        │
│        │                                 │
│        └─→ attempt >= MAX_ATTEMPTS       │
│            └─→ shouldBlock() = true      │
│                └─→ return BLOCKER        │
│                    └─→ 需要人類介入      │
└──────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────┐
│  lib/shared/log-output.cjs               │
│  ────────────────────────────────────────│
│  anchorPass(phase, step, summary, next)  │  ← 成功
│  anchorError(type, msg, action, opts)    │  ← 錯誤
│  anchorOutput({context, error, ...})     │  ← 詳細輸出
│        │                                 │
│        ▼                                 │
│  saveLog(path, content)                  │  ← 保存日誌
│        │                                 │
│        ▼                                 │
│  .gems/iterations/iter-X/logs/           │  ← 日誌目錄
│  ├── BUILD_phase-1_PASS.md               │
│  ├── BUILD_phase-4_PENDING_attempt-2.md  │
│  └── POC_step-2_BLOCKER.md               │
└──────────────────────────────────────────┘
```

---

## 🧩 共用模組依賴圖

```
                    ┌─────────────────────────────────────────┐
                    │          lib/shared/                    │
                    └─────────────────────────────────────────┘
                                      │
        ┌─────────────┬───────────────┼───────────────┬─────────────┐
        ▼             ▼               ▼               ▼             ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ log-output   │ │error-handler │ │output-header │ │next-command  │ │project-type  │
│ .cjs         │ │   .cjs       │ │   .cjs       │ │-helper.cjs   │ │   .cjs       │
├──────────────┤ ├──────────────┤ ├──────────────┤ ├──────────────┤ ├──────────────┤
│anchorOutput()│ │createError   │ │getSimple     │ │getNextCmd()  │ │detectProject │
│anchorPass()  │ │Handler()     │ │Header()      │ │getRetryCmd() │ │Type()        │
│anchorError() │ │shouldBlock() │ │getOutput     │ │getPassCmd()  │ │getSrcDir()   │
│saveLog()     │ │recordError() │ │Header()      │ │              │ │getGreenfield │
│              │ │resetAttempts │ │              │ │              │ │Guide()       │
└──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘
      │                │                                    │
      │                │                                    │
      ▼                ▼                                    ▼
┌────────────────────────────────────────────────────────────────────────────────┐
│                          所有 phases/{phase}/{step}.cjs 都會調用               │
└────────────────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Phase 專用模組

```
┌───────────────────────────────────────────────────────────────────┐
│                          lib/                                     │
├───────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────────────────┐    ┌─────────────────────────┐       │
│  │ lib/checkpoint.cjs     │    │ lib/step-result.cjs     │       │
│  │ ─────────────────────  │    │ ─────────────────────   │       │
│  │ writeCheckpoint()      │    │ writeStepResult()       │       │
│  │ clearCheckpoints()     │    │ readStepResult()        │       │
│  │ 用於: BUILD Phase      │    │ 用於: POC, Ralph Loop   │       │
│  └─────────────────────────┘    └─────────────────────────┘       │
│                                                                   │
│  ┌─────────────────────────┐    ┌─────────────────────────┐       │
│  │ lib/plan/              │    │ lib/scan/               │       │
│  │ ─────────────────────  │    │ ─────────────────────   │       │
│  │ plan-spec-extractor.cjs│    │ gems-scanner.cjs        │       │
│  │ ├─ extractPlanSpec()   │    │ gems-validator.cjs      │       │
│  │ ├─ extractFunction     │    │ gems-patterns.cjs       │       │
│  │ │  Manifest()          │    │ 用於: BUILD 4, SCAN     │       │
│  │ ├─ getStoryContext()   │    └─────────────────────────┘       │
│  │ └─ compareSpecs()      │                                       │
│  │ 用於: BUILD 1,4,7      │                                       │
│  └─────────────────────────┘                                      │
│                                                                   │
│  ┌─────────────────────────┐    ┌─────────────────────────┐       │
│  │ lib/suggestions-       │    │ lib/step-consistency-   │       │
│  │    validator.cjs       │    │    validator.cjs        │       │
│  │ ─────────────────────  │    │ ─────────────────────   │       │
│  │ validate()             │    │ validateStepConsistency │       │
│  │ 用於: BUILD 7          │    │ formatValidationResult  │       │
│  └─────────────────────────┘    │ 用於: BUILD 7          │       │
│                                 └─────────────────────────┘       │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🛠️ 工具腳本 (tools/)

```
tools/
├── quality-check/
│   └── poc-quality-checker.cjs        ← POC Step 2 調用
│       ├── checkPocQuality()
│       └── extractFrontendSpecs()     ← BUILD Phase 1 調用
│
├── auto-runner/
│   └── index.cjs                      ← Ralph Loop 調用 (--new)
│
├── build/
│   ├── test-runner.cjs                ← BUILD Phase 5 調用
│   ├── env-checker.cjs                ← BUILD Phase 1 調用
│   └── route-fixer.cjs                ← BUILD Phase 1 調用 (假路由修正)
│
├── poc/
│   └── migrate-poc-ui.cjs             ← BUILD Phase 1 調用 (UI 移植)
│
├── scan/
│   └── gems-scanner.cjs               ← SCAN 調用
│
├── story-status.cjs                   ← Ralph Loop 調用 (狀態追蹤)
├── draft-parser.cjs                   ← Blueprint Runner 調用 (Enhanced Draft)
└── blueprint-runner.cjs               ← Ralph Loop Blueprint 模式 (v2.1)
```

---

## 🔄 Level 執行順序 (BUILD Phase)

```
Level S (快速原型)
┌─────────────────────────────────────────────────────────┐
│ Phase 1 ──→ Phase 4 ──→ Phase 7                         │
│ (開發)     (標籤驗收)   (完成規格)                       │
│                                                         │
│ 略過: Phase 3, 4, 5, 6, 7                               │
└─────────────────────────────────────────────────────────┘

Level M (標準流程) [預設]
┌─────────────────────────────────────────────────────────┐
│ Phase 1 ──→ Phase 2 ──→ Phase 3 ──→ Phase 4 ──→         │
│ (開發)     (標籤驗收)   (測試腳本)  (Test Gate)         │
│                                                         │
│         ──→ Phase 5 ──→ Phase 8                         │
│            (TDD 執行)   (完成規格)                      │
│                                                         │
│ 略過: Phase 6, 7                                        │
└─────────────────────────────────────────────────────────┘

Level L (嚴格流程)
┌─────────────────────────────────────────────────────────┐
│ Phase 1 ──→ Phase 2 ──→ Phase 3 ──→ Phase 4 ──→         │
│ (開發)     (標籤驗收)   (測試腳本)  (Test Gate)         │
│                                                         │
│         ──→ Phase 5 ──→ Phase 6 ──→ Phase 7 ──→         │
│            (TDD 執行)   (修改測試)  (整合檢查)          │
│                                                         │
│         ──→ Phase 8                                     │
│            (完成規格)                                   │
└─────────────────────────────────────────────────────────┘
```

---

## 📋 狀態文件 & 產出物

| 文件 | 位置 | 產生者 | 用途 |
|------|------|--------|------|
| `story_status.json` | `.gems/` | Ralph Loop | Story 進度 |
| `blueprint_state.json` | `.gems/` | Blueprint Runner | 藍圖進度 |
| `last_step_result.json` | `.gems/` | step-result.cjs | 最後步驟結果 |
| `checkpoint_*.json` | `.gems/iterations/iter-X/build/` | checkpoint.cjs | 步驟檢查點 |
| `functions.json` | `.gems/` | SCAN | GEMS 函式清單 |
| `Fillback_Story-X.Y.md` | `.gems/iterations/iter-X/build/` | Phase 7 | 開發回填 |
| `iteration_suggestions_*.json` | `.gems/iterations/iter-X/build/` | Phase 7 | 迭代建議 |

---

*文檔生成時間: 2026-01-30 13:30*
